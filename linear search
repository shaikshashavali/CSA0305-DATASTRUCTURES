#include<stdio.h>
#include<conio.h>
int main(){
int a[10],key,i,size,index;
printf("enter the no of elements:");
scanf("%d",&size);
printf("enter the elements in an array:");
for(i=0;i<size;i++)
scanf("%d",&a[i]);
printf("enter the search key value:");
scanf("%d",&key);
for(index=0;index<size;index++)
if(a[index]==key)
break;
if(index<size)
printf("the key is found at index %d",index);
else
printf("the key is not found");
return 0;
}
